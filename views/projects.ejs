<div class="flex flex-col w-full">
  <h2 class="font-bold text-2xl self-end text-primary mb-4">projects</h2>
  <% projects.forEach(project => { %>
    <%- include('partials/projectCard', { project }) %>
  <%}) %>

</div>
